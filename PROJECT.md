# 旷野淘金 · 校园测试开发包 — 项目文档

> 源目录：`金币路径规划_校园测试开发包`
> 本文档整理时间：2026-09-07

---

## 一、项目概述

"旷野淘金"是一个面向校园的在线浏览器路径规划测试系统。学生操控小车在网格地图上移动，收集金币、避开障碍，学习从"任意拾取"到"给定顺序"再到"自主优化"的路径规划能力。

**核心技术约束：**
- 只有四向单格移动（up/down/left/right），无对角线
- 静态障碍，自动收集金币（经过即拾取）
- 最后一枚金币到手立即结束，不返回起点
- 256 条命令上限（含碰撞）

---

## 二、关卡体系

### 2.1 总体结构

**20 张地图 × 2 种操作模式 = 40 个任务**

| 模式 | 编号 | 输入方式 |
|---|---|---|
| 键盘 | K01–K20 | 方向键/WASD/四向按钮 |
| Python | P01–P20 | 填空模板（方向 ± 重复次数）|

Knn 与 Pnn 共用同一张地图（L01–L20），起点、障碍、金币、目标完全相同。

### 2.2 三阶段递进

| 阶段 | 关卡 | 特点 |
|---|---|---|
| 探索阶段 | 01–06 | 单方向→拐弯→多金币→首次障碍，任意顺序拾取 |
| 教学阶段 | 07–12 | 给定金币拾取顺序，有示范路线和知识检查 |
| 优化阶段 | 13–20 | 自主选择顺序，最多3轮，比较首轮与最佳 |

### 2.3 金币数量递进

| 关卡段 | 金币数 |
|---|---|
| 01–03 | 1 枚 |
| 04–07 | 2 枚 |
| 08–10 | 3 枚 |
| 11–14 | 4 枚 |
| 15–17 | 5 枚 |
| 18–20 | 6 枚 |

---

## 三、坐标与规则内核

### 3.1 坐标系

- 原点左下，x 向右增加，y 向上增加
- 屏幕行号 `row = height - 1 - y`

### 3.2 状态转移（纯函数）

```
step(state, command, rules) → { next_state, domain_events }
```

处理顺序：验证轮次/命令 → 边界/障碍检查 → 更新位置和步数 → 自动拾取 → 顺序检查 → 收齐检查 → 命令预算 → 原子提交事件

### 3.3 碰撞规则

- 撞墙/越界：位置不变，命令数+1，碰撞数+1，成功步数不加
- 继续接受下一条命令，不自动修正

### 3.4 拾取规则

- 进入金币格立即拾取，移除金币，再次经过不重复得分
- 给定顺序关：提前捡到非下一枚金币 → 先记录移动和拾取，再终止为 `order_violation`
- 无顺序限制关：以真实拾取顺序为准

### 3.5 终止条件

| 状态 | 含义 |
|---|---|
| `success` | 所有金币收齐 |
| `order_violation` | 拾取顺序违反给定约束 |
| `command_limit` | 256条命令用尽未完成 |
| `incomplete` | 代码执行完毕但未收齐 |
| `stopped` | 玩家主动停止 |
| `abandoned` | 重来保留旧轮 |
| `expired` | 场次截止 |

---

## 四、BFS 最优解算法

### 4.1 自由顺序 BFS

状态：`(x, y, collected_mask)`

- collected_mask 是位掩码，每位代表一枚金币是否已收集
- 每条成功移动成本 1
- 走到金币格立即更新 mask
- 首次 `mask == full_mask` 即为最优解
- 最多 8×8 地图、6 枚金币 → 最多 512 个搜索状态

### 4.2 给定顺序 BFS

在自由顺序基础上增加约束：每次新拾取的金币必须是顺序中的下一枚。经过"未来金币"的分支不能成为合法解。

### 4.3 参考最优值

| 地图 | L01 | L02 | L03 | L04 | L05 | L06 | L07 | L08 | L09 | L10 |
|---|---|---|---|---|---|---|---|---|---|---|
| 最短步数 | 2 | 3 | 5 | 9 | 8 | 8 | 9 | 17 | 13 | 15 |

| 地图 | L11 | L12 | L13 | L14 | L15 | L16 | L17 | L18 | L19 | L20 |
|---|---|---|---|---|---|---|---|---|---|---|
| 最短步数 | 23 | 20 | 20 | 17 | 30 | 26 | 30 | 35 | 36 | 40 |

---

## 五、Python 输入系统

### 5.1 两种模板

| 模板 | 适用关卡 | 学生可编辑 | 行数限制 |
|---|---|---|---|
| `call_slots_v2` | P01–P04 | 每行 direction 空 | P01:2行, P02:3行, P03:5行, P04:1-32行 |
| `repeat_slots_v2` | P05–P20 | 每段 direction + count 空 | 1-24段，展开1-256步 |

### 5.2 代码白名单

- 函数：`move_up()`, `move_down()`, `move_left()`, `move_right()`
- 循环（P05+）：`for _ in range(n):` 其中 n 为 0–20 的整数
- 不开放：变量、if/while、import、嵌套循环、任意表达式
- 不提供：`move_to`、自动寻路函数

### 5.3 输入校验

- direction：英文小写白名单 (up/down/left/right)
- count：0–20 的十进制整数，不接受负数、小数、表达式
- 0 跳过该段，整个程序至少 1 步且最多 256 步
- 不用 eval/exec 执行学生文本，使用封闭模板模式

---

## 六、评分系统

### 6.1 计分公式（13–20关）

```
收集分 = 60 × 已收集唯一金币数 / 总金币数
路线分 = success ? 40 × 本关最短步数 / 成功移动步数 : 0
总分   = 收集分 + 路线分
```

展示 1 位小数，保留全精度。

### 6.2 示例（L14）

- 成功 15 步（最优）= 60 + 40 = 100 分
- 成功 19 步 = 60 + 40×15/19 ≈ 91.6 分
- 只捡 2/3 枚 = 40 + 0 = 40 分

### 6.3 最佳轮次规则

1. 成功 > 未成功
2. 成功轮：步数最少者
3. 未成功轮：收集数最多者
4. 同分：保留较早轮作代表
5. 首轮不能被最佳覆盖

---

## 七、玩家身份与埋点

### 7.1 UUID 玩家系统

- 首次进入自动创建随机 UUIDv4 + 随机称呼（如"淘金者 Q7M2"）
- 跨刷新/关卡/模式保持同一身份
- "换一位玩家"：确认后新建，旧数据保留隔离
- UUID 是标识，不是授权凭证

### 7.2 全量埋点

**77 种交互位置模式 + 32 种系统节点**

双层记录：
- **原始交互层**：pointerdown/up、click、keydown/up、input、scroll 等
- **语义规则层**：进入关卡、选择顺序、修改代码、命令派发、成功移动、撞墙、拾取等

两层以 `interaction_id` 串联。一次按键可能产生 5 条日志。

### 7.3 事件 envelope 结构

```json
{
  "schema_version": "2.0.0",
  "event_id": "事件UUID",
  "player_uuid": "玩家UUID",
  "session_id": "场次UUID",
  "assignment_key": "P14",
  "level_id": "L14",
  "mode": "python_blank",
  "attempt_id": "轮次UUID",
  "stream_id": "流UUID",
  "seq": 127,
  "event_type": "python.field_edited",
  "element_id": "python.row.row-uuid-3.count",
  "interaction_id": "交互UUID",
  "client_time": "ISO UTC",
  "mono_ms": 25120.31,
  "elapsed_ms": 93450.00,
  "payload": { ... }
}
```

### 7.4 五种事件流

| 流 | 生命周期 | 内容 |
|---|---|---|
| bootstrap | 页面首次登记前 | 页面打开、身份创建 |
| player/session | 已登记玩家 | 入口导航、场次操作 |
| assignment | 进入某关至离开 | 读题、计划、代码编辑 |
| attempt | 一次运行开始至终止 | 执行输入、命令、移动、结果 |
| system | 每页面运行期 | 网络、存储、同步、异常 |

---

## 八、技术架构

```
键盘适配器 / Python 填空编译器
              ↓ 标准命令
          纯函数规则内核 → 已提交状态 → DEMO 视觉组件
              ↓ 领域事件
     全量交互记录 + 事件事务 → IndexedDB → 批量上传
              ↓
服务器绑定身份 / 版本重放 / 完整性核对 → 权威成绩 → 教师导出与回放
```

### 技术栈

- 前端：React + TypeScript
- 后端：Node.js + TypeScript
- 数据库：SQLite（单实例）或 PostgreSQL（无持久磁盘时）
- 前端本地存储：IndexedDB

### API 接口

| 接口 | 用途 |
|---|---|
| `POST /api/players/bootstrap` | 自动创建/恢复 UUID 玩家 |
| `POST /api/players/switch` | 切换玩家 |
| `POST /api/join` | 加入场次 |
| `GET /api/assignments/:id` | 获取任务（不含答案）|
| `POST /api/attempts` | 开始轮次 |
| `POST /api/event-streams/:id/batches` | 批量上传事件 |
| `POST /api/attempts/:id/finalize` | 结束轮次，服务端重放验证 |
| `POST /api/teacher/sessions` | 教师创建场次 |
| `GET /api/teacher/sessions/:id/export` | 教师导出数据 |

---

## 九、教学设计亮点

### 9.1 教学阶段示范（07–12关）

- 提供短路线（最优）和长路线（多2步）两条示范
- 逐格播放/暂停/重放
- 知识检查："两条路线都按要求捡完了，哪条短？为什么？"
- 示范走独立 demo 流，不污染玩家轮次

### 9.2 L12 特殊说明

要求 A→B→C→D，但 A 到 B 的直接路线会经过 C。学生必须理解"途中自动拾取"的影响，不能简单按端点距离计算。

### 9.3 L14 教学价值

"最近的一枚不保证全程最短"：
- 最近邻 C→B→A = 19 步
- 更优 B→C→A = 15 步

### 9.4 C++ 学生过渡

P05 前提供 C++/Python 对照：
```cpp
for (int i = 0; i < 3; i++) { move_right(); }
```
```python
for _ in range(3): move_right()
```

---

## 十、开发包文件清单

| 文件 | 用途 |
|---|---|
| `开发文档_V2.md` | 主规格文档（326行）|
| `20关设计与Python输入.md` | 逐关详细设计（806行）|
| `全量埋点与玩家标识.md` | UUID 与埋点规格（169行）|
| `玩家交互埋点清单.md` | 77类交互位置清单 |
| `interaction_catalog.json` | 交互注册表（机器可读）|
| `levels.teacher.json` | 20地图/40任务配置 |
| `solutions.teacher.json` | 20最优解/80种顺序/6组教学 |
| `python_starters/` | P01–P20 填空模板 |
| `teacher_answers/` | P01–P20 合法参考解 |
| `maps_v2/` | L01–L20 地图 SVG |
| `reference_core.py` | BFS 求解器 + 回放验证器 |
| `build_v2.py` | 内容资产生成器 |
| `build_assets.py` | 资产构建入口 |
| `verify_assets.py` | 资产校验入口 |
| `telemetry.example.jsonl` | 事件样例（K01两步成功）|
| `demo_assets.manifest.json` | 贴图来源/SHA-256 |
| `verification_report.json` | 当前校验结果 |
| `DEMO/` | 原始 HTML 示例与贴图 |
| `assets_demo/` | 提取的6份PNG素材 |

---

## 十一、验收要点（P0 必须通过）

| ID | 验收项 |
|---|---|
| C01 | 20地图、K20/P20、逐关可编辑边界 |
| C02 | 40个任务都能合法完成 |
| C03 | 墙、越界、自动拾取、未来金币、256边界 |
| C04 | Python方向/次数/行数/模板按关锁定 |
| C05 | 07–12有示范比较和知识检查 |
| C06 | 13–20有计划、三轮、首轮保留 |
| T01 | 77种交互位置模式全部有埋点 |
| S01 | 服务端重放验证，不信任客户端 |
| E01 | 教师开场/收卷/导出/回放 |
| L01 | 100客户端10分钟压测 |

---

## 十二、抽测配置

- 完整 40 关全量开发
- 40 分钟现场抽测：每种模式 01、04、06、09、12、14、20 共 7 关，两模式共 14 关
- 未分配关卡记 `not_assigned`，不记未通过或零分
- 题库数量不随抽测范围缩减
